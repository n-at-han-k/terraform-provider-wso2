// Code generated by OpenAPI Generator (https://openapi-generator.tech); DO NOT EDIT.
package provider

import (
	"encoding/json"

	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-framework-jsontypes/jsontypes"

	"github.com/n-at-han-k/terraform-provider-wso2/internal/client"
)

// IdentityProvidersMetaFederatedAuthenticatorModel is the Terraform model for identity_providers_meta_federated_authenticator.
type IdentityProvidersMetaFederatedAuthenticatorModel struct {
	AuthenticatorId types.String `tfsdk:"authenticator_id"`
	Name types.String `tfsdk:"name"`
	DisplayName types.String `tfsdk:"display_name"`
	DefinedBy types.String `tfsdk:"defined_by"`
	Tags jsontypes.Normalized `tfsdk:"tags"`
	Properties jsontypes.Normalized `tfsdk:"properties"`
	FederatedAuthenticatorId types.String `tfsdk:"federated_authenticator_id"`
}



// FromClientModel updates the Terraform model from a client model.
func (m *IdentityProvidersMetaFederatedAuthenticatorModel) FromClientModel(c *client.MetaFederatedAuthenticator) {
	m.AuthenticatorId = types.StringValue(c.AuthenticatorId)
	m.Name = types.StringValue(c.Name)
	m.DisplayName = types.StringValue(c.DisplayName)
	m.DefinedBy = types.StringValue(c.DefinedBy)
	// Marshalling a Go value cannot fail in a way worth surfacing here; an
	// unrepresentable one would have failed on the way in.
	//
	// The answer is only written when it says something the configuration does
	// not already say -- see jsonSupersetOf. A server that merely filled in its
	// own defaults has told us nothing, and recording it would fail the apply
	// and then propose an update forever.
	if encoded, err := json.Marshal(c.Tags); err == nil {
		if m.Tags.IsNull() || m.Tags.IsUnknown() ||
			!jsonSupersetOf(string(encoded), m.Tags.ValueString()) {
			m.Tags = jsontypes.NewNormalizedValue(string(encoded))
		}
	}
	// Marshalling a Go value cannot fail in a way worth surfacing here; an
	// unrepresentable one would have failed on the way in.
	//
	// The answer is only written when it says something the configuration does
	// not already say -- see jsonSupersetOf. A server that merely filled in its
	// own defaults has told us nothing, and recording it would fail the apply
	// and then propose an update forever.
	if encoded, err := json.Marshal(c.Properties); err == nil {
		if m.Properties.IsNull() || m.Properties.IsUnknown() ||
			!jsonSupersetOf(string(encoded), m.Properties.ValueString()) {
			m.Properties = jsontypes.NewNormalizedValue(string(encoded))
		}
	}
}
